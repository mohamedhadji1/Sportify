apiVersion: v1
kind: Service
metadata:
  name: tournament-service
  namespace: default
  labels:
    app: tournament-service
spec:
  selector:
    app: tournament-service
  ports:
    - protocol: TCP
      port: 4100
      targetPort: 4100
  type: ClusterIP
